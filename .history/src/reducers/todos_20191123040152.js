const todos = (state = [], action) => {
    switch(action.type){
        case 'ADD_TODO':
            return [
                ...sr
            ]
        default:
            return state
    }
}

export default todos